const SECONDS = 1000
const MINUITS = SECONDS * 60
const HOURS = 60 * MINUITS
const DAYS = HOURS * 24

const REFRESH_RATE = 5 //units

const STARTING_BALANCE = 50

module.exports = {
    SECONDS,
    MINUITS,
    HOURS,
    DAYS,
    REFRESH_RATE,
    STARTING_BALANCE
}